class Template {
public:
    void process(T item);
};
```

---

### JavaScript / TypeScript

**文件后缀**: `.js`, `.ts`, `.jsx`, `.tsx`

**一级检测**：通过文件扩展名 `.js`, `.ts`, `.jsx`, `.tsx` 立即识别为 JavaScript/TypeScript。

**二级检测**：至少匹配以下 1 个特征

```javascript
